<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Allerta&family=Kodchasan:wght@400;500;700&display=swap" rel="stylesheet" />
  <title>SetEduca - Painel Financeiro</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Kodchasan', sans-serif;
      background-color: #fff4df;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .logo-container {
      display: flex;
      justify-content: flex-end;
      margin-top: 15px;
      padding-right: 15px;
      z-index: 1;
    }

    .logo {
      width: 90px;
      height: auto;
      max-width: 100%;
      position: relative;
      top: -15px;
    }

    header {
      background-color: #59C631;
      border-radius: 0 0 40px 40px;
      padding: 10px 20px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      height: 60px;
      margin-top: -15px;
      position: relative;
    }

    .header-title {
      position: absolute;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
      font-size: 1.5rem;
      color: black;
      font-weight: normal;
      font-family: 'Kodchasan', sans-serif;
      white-space: nowrap;
    }

    .header-title b {
      font-family: 'Allerta', sans-serif;
    }

    main {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: calc(100vh - 120px);
      padding: 30px 20px;
    }

    .button-wrapper {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-wrap: wrap;
      gap: 30px;
      max-width: 900px;
      width: 100%;
    }

    .container {
      width: 280px;
      height: 180px;
    }

    .action-button {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background-color: #59C631;
      color: #000;
      text-decoration: none;
      font-weight: bold;
      border-radius: 20px;
      padding: 20px;
      border: 2px solid #268C41;
      transition: all 0.3s;
      height: 100%;
      box-sizing: border-box;
      text-align: center;
      gap: 15px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      font-size: 1.1rem;
    }

    .action-button:hover {
      background-color: #4ca529;
      transform: translateY(-3px);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
    }

    .action-button img {
      width: 50px;
      height: 50px;
    }

    .action-button span {
      font-size: 1.2rem;
      font-family: 'Allerta', sans-serif;
    }

    @media (max-width: 650px) {
      .button-wrapper {
        flex-direction: column;
      }
      
      .container {
        width: 100%;
        max-width: 280px;
      }
    }
  </style>
</head>
<body>
  <div class="logo-container">
    <img class="logo" src="/imagem/logocomp.png" alt="Logo" />
  </div>

  <header>
    <div class="header-title">
      painel <b>FINANCEIRO</b>
    </div>
  </header>

  <main>
    <div class="button-wrapper">
      <section class="container">
        <a class="action-button" id="link1" href="/adimplentes-inadimplentes">
          <img src="/imagem/financeiro/cardholder.svg" alt="Adimplentes e Inadimplentes" />
          <span>Adimplentes e Inadimplentes</span>
        </a>
      </section>
      <section class="container">
        <a class="action-button" id="link2" href="/fluxo-de-caixa">
          <img src="/imagem/financeiro/Vector.png" alt="Fluxo de Caixa" />
          <span>Fluxo de Caixa</span>
        </a>
      </section>
      <section class="container">
        <a class="action-button" id="link3" href="/relatorio-financeiro">
          <img src="/imagem/financeiro/Vector (1).png" alt="Relatório" />
          <span>Relatório</span>
        </a>
      </section>
    </div>
  </main>
</body>
</html>
